import SectionTitle from '../SectionTitle'

export default function Projects() {
	return (
		<section id="projects" className="h-full w-full pt-32">
			<SectionTitle title="Proyectos" />
		</section>
	)
}
